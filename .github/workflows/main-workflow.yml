name: Build, test and push Docker Images

on:
  workflow_dispatch:

jobs:
  base:
    uses: ./.github/workflows/main-workflow-template.yml
    with:
      image: base
      test: true
      push: false
  base:
    uses: ./.github/workflows/main-workflow-template.yml
    with:
      image: python
      test: false
      push: false